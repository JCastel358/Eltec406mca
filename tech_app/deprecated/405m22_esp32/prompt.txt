can you make sure that the @tech_app/405m22_esp32/  app is also good to go. also please update @status.md with the latest. I have fixed the spike by isolating the emitter onto its own battery. i am now using the 6.5V battery for the emitters only and a 9V battery for the sensors (this is the battery that is also used for AIN1 and AIN7 (the battery reader) instead of the 6.5 v for everything. also the testing has changed a bit, i now need check for sensitivity and then once we have that sensitivity (we are still checking for offset, polarity, and peak to peak sensitivity, i will get the failing values are:  we need to check for noise right after.